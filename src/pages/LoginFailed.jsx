import React from 'react';

const LoginFailed = () => {
  return (
    <section className="loginFailed">
      <a href="/login">Login Failed! Click here to Login again</a>;
    </section>
  );
};

export default LoginFailed;
